{% set logo = theme_var(mobile ? 'custom_logo_mobile' : 'custom_logo') %}
<a href="{{ home_url }}" class="navbar-brand">
<img src="{{ url('theme://images/underdogs-logo.png')  }}" alt="{{ site.title }}" />
</a>